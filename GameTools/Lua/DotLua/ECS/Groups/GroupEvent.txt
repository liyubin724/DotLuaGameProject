local oop = require('DotLua/OOP/oop')

local GroupEvent =
    oop.enum(
    'DotLua.ECS.Groups.GroupEvent',
    {
        Added = 1,
        Removed = 2,
        AddOrRemove = 3,
    }
)

return GroupEvent
