local oop = require('DotLua/ECS/OOP/oop')
local FSMToken = require ('DotLua/ECS/Systems/FSM/Token/FSMToken')

local FSMActionToken =
    oop.class(
    'DotLua.ECS.Systems.FSM.Token.FSMActionToken',
    function(self, name)
    end,
    FSMToken
)

return FSMActionToken
