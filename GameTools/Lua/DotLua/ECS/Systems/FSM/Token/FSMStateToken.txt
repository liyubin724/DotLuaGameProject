local oop = require('DotLua/ECS/OOP/oop')
local FSMToken = require('DotLua/ECS/Systems/FSM/Token/FSMToken')

local FSMStateToken =
    oop.class(
    'DotLua.ECS.Systems.FSM.Token.FSMStateToken',
    function(self, name)
    end,
    FSMToken
)

return FSMStateToken
