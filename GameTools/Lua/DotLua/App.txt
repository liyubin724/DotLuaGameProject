local oop = require("DotLua/OOP/oop")
local FrameUpdateSystem = require "DotLua.ECS.Systems.Update.FrameUpdateSystem"

_G.serpent = require("DotLua/Depends/Serpent/serpent")

_G.AppTime = _G.AppTime or {
    deltaTime = 0,
    unscaleDeltaTime = 0,
    timeSinceStartup = 0,
    unscaleTimeSinceStartup = 0,
}

oop.asreadonly(_G.AppTime)