local oop = require("DotLua/OOP/oop")

local tremove = table.remove
local tinsert = table.insert
local tclear = table.clear

local ObjectPool =
    oop.class(
    "DotLua.Pool.ObjectPool",
    function(self, itemClass)
        self.itemClass = itemClass
        self.unusedItems = {}
    end
)

function ObjectPool:Get(...)
    local item

    if #(self.unusedItems) > 0 then
        item = self.unusedItems[1]
        tremove(self.unusedItems, 1)
    else
        item = self.itemClass()
    end

    if item["DoGet"] then
        item["DoGet"](item, ...)
    elseif item["DoActivate"] then
        item["DoActivate"](item, ...)
    end

    return item
end

function ObjectPool:Release(item, ...)
    if item then
        if item["DoRelease"] then
            item["DoRelease"](item, ...)
        elseif item["DoDeactivate"] then
            item["DoDeactivate"](item, ...)
        end

        tinsert(self.unusedItems, item)
    end
end

function ObjectPool:Clear()
    tclear(self.unusedItems)
end

return ObjectPool
