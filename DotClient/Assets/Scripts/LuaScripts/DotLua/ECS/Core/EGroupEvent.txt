local oop = require('DotLua/OOP/oop')

local EGroupEvent =
    oop.enum(
    'DotLua.ECS.Core.EGroupEvent',
    {
        Added = 1,
        Removed = 2,
        AddedOrRemoved = 3,
    }
)

return EGroupEvent
