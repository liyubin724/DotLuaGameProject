local oop = require("DotLua/OOP/oop")

local LogTag = "EntitySelector"

local EntitySelector =
    oop.class(
    "DotLua.ECS.Core.EntitySelector",
    function(self, name)
        self.name = name
    end
)

function EntitySelector:GetName()
    return self.name
end

function EntitySelector:GetResult(...)
    oop.error(LogTag, "The method should be implement")
    return nil
end

function EntitySelector:TryToUpdateEntity(entity, contextEvent, param1, param2)
    oop.error(LogTag, "The method should be implement")
    return nil
end

return EntitySelector
