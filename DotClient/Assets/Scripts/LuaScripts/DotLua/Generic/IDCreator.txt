local oop = require("DotLua/OOP/oop")

local IDCreator =
    oop.class(
    "DotLua.Generic.IDCreator",
    function(self)
        self.cachedID = 0
    end
)

function IDCreator:Get()
    self.cachedID = self.cachedID + 1

    return self.cachedID
end

return IDCreator