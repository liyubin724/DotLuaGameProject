local oop = require('DotLua/OOP/oop')
local InitializeSystem = oop.using('DotLua/ECS/Systems/LifeCycle/InitializeSystem')

local DEUIRoot = CS.DotEngine.UI.UIRoot
local gameECS = game.ecs

local StartupSystem =
    oop.class(
    'StartupSystem',
    function(self)
    end,
    InitializeSystem
)

function StartupSystem:DoInitialize()
    -- local uiContext = gameECS:GetUIContext()
    -- local uiRootEntity = uiContext:CreateEntity(gameECS:GetNextUID())
    -- uiRootEntity:AddComponent(DEUIRoot.Root:GetRootComponent())
    -- uiContext:SetRootEntity(uiRootEntity)
end

return StartupSystem
